{
  "project_name": "{{ item.project_name }}",
  "storage_limit": {{ item.storage_limit | default(0) | int }},
  "metadata": {
    "public": "{{ item.is_public | default ("true") }}",
    "enable_content_trust": "{{ item.content_trust | default ("false") }}",
    "prevent_vul": "{{ item.prevent_vul | default ("true") }}",
    "severity": "{{ item.severity | default ("high") }}",
    "auto_scan": "{{ item.auto_scan | default ("true") }}"
  }
}
